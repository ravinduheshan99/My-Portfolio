import type { Metadata } from 'next'
import { GeistSans } from 'geist/font/sans'
import { GeistMono } from 'geist/font/mono'
import './globals.css'

export const metadata: Metadata = {
  title: 'Ravindu Haputhanthri',
  description:
    'Software Engineer – Quality Assurance (ISTQB-CTFL) with experience in enterprise QA across manual, automation, API, performance, and release validation. DevOps enthusiast focused on CI/CD-driven quality engineering.',
  icons: {
    icon: '/favicon.ico',
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable}`}>
        {children}
      </body>
    </html>
  )
}
